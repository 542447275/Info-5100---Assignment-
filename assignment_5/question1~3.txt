1. setName() function doesn't assign the input name to the object‘s name, it just return the input name instead.
   Two Book() constructors' input parameter are the same type (int), which will make user confused. 

2. The return type of getTime() function should be String.

3.(1) 0
      0
      10
      0
      20
      30

  (2) defined two "ONE"s.